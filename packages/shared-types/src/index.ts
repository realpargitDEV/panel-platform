/**
 * Wire types shared with the agent.
 *
 * Everything here is generated from `crates/api-types` by `pnpm contracts`.
 * Edit the Rust types and regenerate — `pnpm contracts:check` fails CI if this
 * package is out of date.
 */
export * from './generated.js';
