{
  "name": "@project-host/validation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Form-level validation for the desktop client. The agent remains the authority.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@project-host/api-contracts": "workspace:*",
    "zod": "^3.24.1"
  }
}
